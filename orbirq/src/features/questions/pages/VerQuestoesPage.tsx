import QuestoesList from "../components/QuestoesList"

export default function VerQuestoesPage() {
  // A página agora simplesmente renderiza o componente QuestoesList
  // que já contém toda a lógica de carregamento e exibição das questões
  return <QuestoesList />
}
